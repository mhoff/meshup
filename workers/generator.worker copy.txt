import { expose } from 'comlink';

export function calc(): number {
  console.log('calc');
  let result = 1;
  for (let i = 0; i < 1000; i++) {
    result *= i;
  }
  return result;
}

const exports = {
  calc,
};
export type MatcherWorker = typeof exports;

expose(exports);
